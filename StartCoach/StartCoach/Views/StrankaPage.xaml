<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:StartCoach.ViewModels"
             mc:Ignorable="d"
             x:Class="StartCoach.Views.StrankaPage">

    <ContentPage.BindingContext>
        <vm:StrankaViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>

            <Button x:Name="PressMeButton"
                Text="Start/Stop!"
                Pressed="ToggleAccelerometer"/>

            <Label x:Name="LabelX" HorizontalOptions="Center"/>
            <Label x:Name="LabelY" HorizontalOptions="Center"/>
            <Label x:Name="LabelZ" HorizontalOptions="Center"/>

            <Label Text="{Binding Test}" HorizontalOptions="Center"/>

            <Button
                Text="Start/Stop!"
                Command="{Binding testCommand}"/>



        </StackLayout>
    </ContentPage.Content>
</ContentPage>